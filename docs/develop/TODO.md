# OpenFic 改进 TODO

> 检查日期：2026-09-05。主工作目录：`G:\code\OpenFic`，分支 `main`，基线 `cb27371`。
> 对照资料：根目录 README、`docs/develop/feature-map.md`、`docs/superpowers/specs/` 中的需求规格，以及相关前后端、桌面端源码。
> 当前执行分支：`codex/todo-improvements`。清单保留最初静态检查的证据，并在已完成项目下记录实现和定向验证；未勾选项目仍需按其边界另行实施。体积数据以本分支实测结果为准。

## 优先顺序

| 优先级 | 建议先做 | 理由 |
| --- | --- | --- |
| P1 | T01 保存结果、T02 缓存更新、T03 启动依赖 | 改善数据状态可信度、升级一致性和进入写作界面的速度 |
| P1 | T04 整理分支、T05 已确认笔记需求 | 把已经讨论或实现的功能落实到可用版本 |
| P2 | T06–T10 体积与模块边界 | 有明确入口，分阶段实施，先测量再裁剪 |
| P2 | T11–T14 交互、检索和文档 | 降低误操作、查找成本与后续维护成本 |
| P3 | T15–T16 记忆管理与备份体验 | 新功能候选，需先明确产品边界 |

复选框表示验收完成；“已有实现待集成”不等于“需要重新开发”。本清单是建议，不代表已批准实现所有新增功能。

## P1：优先处理

### T01 自动保存必须区分成功、失败和跳过

- [x] 修正保存结果契约，并让界面明确区分本地草稿与服务端已保存。
- **完成记录（2026-09-05）**：自动保存回调现在返回 `saved / skipped / failed`，只有服务端确认成功才推进保存时间；章节和笔记失败后保持未保存状态与本地 working copy。
- **证据**：`frontend/src/features/writing/hooks/use-auto-save.ts` 的 `save()` 在 `await onSave()` 后直接写入 `lastSaveTime`；`chapter-editor.tsx` 的 `handleSave` 捕获异常后只同步脏状态，`note-editor.tsx` 捕获后只保留未保存状态，没有向 Hook 返回失败。锁定、内容超限等分支也可能正常返回。
- **影响**：Hook 可能把失败或跳过的保存记录为成功时间。是否已被所有页面展示，需要检查调用方；不能据此断言正文已丢失，现有 working copy 已提供本地草稿保护。
- **建议**：保存回调返回明确结果，例如 `saved / skipped / failed`；仅在服务端成功确认后更新时间。失败保留草稿和重试入口。后续把章节、笔记重复的保存收尾逻辑收敛为小型公共 Hook。
- **定位**：上述三个文件；`frontend/src/features/writing/hooks/use-writing-working-copy.ts`。
- **最小验收**：一次正常保存、一次断网失败、一次锁定跳过；失败后时间不推进，草稿仍在，重试可成功。适合一条聚焦保存结果的测试，不扩展全套编辑器 E2E。
- **工作量**：中；涉及保存语义，先修结果契约，再考虑抽取公共逻辑。

### T02 修正 PWA 缓存版本和资源范围

- [x] 为每次构建生成缓存版本，限定静态资源缓存范围，并清理旧版本。
- **完成记录（2026-09-05）**：构建后处理根据全部发行文件内容生成缓存指纹；Service Worker 仅运行时缓存明确的静态路径，并排除封面、角色图片和 Agent 附件。同步修复 `font-faces.css` 被重复处理导致构建结果不稳定的问题。
- **证据**：`frontend/public/sw.js` 固定使用 `openfic-shell-v1`，激活时只删除名称不同的 shell 缓存；带 hash 的旧资源因此不会随每次构建自动清理。普通同源 GET 成功响应也会写入缓存。`BACKEND_PATHS` 只列了 `/api/`、`/socket.io/`、`/covers/`、`/icons/`，没有覆盖配置中已有的 `/character-images/`、`/agent-attachments/`。
- **影响**：长期升级可能积累旧静态资源；被运行时缓存的图片、附件可能在服务端变更后仍命中旧响应。固定文件名的字体也需要版本策略。
- **建议**：缓存名带构建 ID；仅缓存明确允许的应用静态资源，用户资产走独立策略或网络请求。更新时保留正在编辑页面需要的旧资源，避免新旧页面和缓存交接造成加载失败。
- **定位**：`frontend/public/sw.js`、`frontend/scripts/generate-precachelist.mjs`、`frontend/src/pwa/register-sw.ts`、`frontend/vite.config.ts`。
- **最小验收**：连续两个构建升级、打开旧标签页、离线重载各检查一次；确认新版本可用、旧缓存可回收、资产不会被误当作固定应用文件。
- **工作量**：中；这是缓存机制问题，不应仅修改包体积警告阈值。

### T03 将 Token 编码表移出应用启动必需路径

- [x] 应用主体先可用，Token 编码表后台初始化，精确统计就绪后再更新。
- **完成记录（2026-09-05）**：编码表改为后台预加载；失败只记录警告并继续使用现有字符估算，不再触发整个应用初始化重试。
- **证据**：`frontend/src/main.tsx` 的启动 `Promise.all` 等待 `preloadTiktokenEncoding()`，失败会进入整个应用初始化重试。`frontend/src/lib/tiktoken-utils.ts` 已通过动态 import 加载编码表，并在未就绪时返回字符长度估算，因此“模块已拆包”并没有消除启动等待。
- **建议**：复用现有估算回退，增加“估算/精确”状态；不要因编码表加载失败阻止用户打开作品。服务端上下文预算的权威计算保持独立。
- **定位**：上述两个文件及 Token 统计的调用方。
- **最小验收**：阻止编码表资源加载时仍能进入项目并编辑；恢复资源后统计可更新。比较冷启动到编辑可用的时间。
- **工作量**：小到中。

### T04 标明并处理已有功能分支，避免重复开发

- [x] 分别检查已有分支的验收状态，再安排集成并更新功能清单。
- **完成记录（2026-09-05）**：快捷新会话和 Agent 消息导航分支均已合并到 `codex/todo-improvements`；合并后的前端类型检查、Lint 和生产构建通过。
- **证据**：本次 `git worktree list` 显示主分支仍在 `cb27371`；消息导航位于 `codex/agent-message-navigator`，提交 `3a58b00`；快捷新会话位于 `feat/quick-start-session`，提交 `5eb4cce`。两者不能当成主目录已具备的功能。
- **建议**：在集成记录中分别列出实现、已验证项和遗留项。消息导航分支仍应补充人工悬停预览、真实流式输出时跳转的验收记录；此前 ResizeObserver 提示应先定位来源，不直接归因于新功能或忽略。本次未重新验收快捷新会话分支。
- **定位**：`.worktrees/agent-message-navigator`、`.worktrees/quick-start-session`；对应 `docs/superpowers/specs/` 设计文件。
- **最小验收**：集成后主工作目录可定位到相关实现；已有快捷新会话行为与导航行为均可使用，文档状态一致。
- **工作量**：小到中，取决于分支差异；本次未执行合并。

### T05 完成跨项目笔记导入与同级混合排序

- [x] 按已确认规格实现跨项目选择导入、冲突预览和笔记树持久化排序。
- **完成记录（2026-09-05）**：增加 `order_index` 迁移、原子混合排序接口、跨项目选择预览/事务复制及前端导入入口；来源项目只读，新笔记使用默认状态，覆盖保留目标状态与位置。保留原 Markdown/ZIP 导入。仅增加 3 条数据安全相关后端测试。
- **证据**：`note-import-dialog.tsx` 仍只接受 `.md/.zip`，`notes.py` 仍提供文件导入链路；`note-tree.tsx` 已有拖拽到分类的移动逻辑，但不等于同级任意位置排序。设计文件已明确两者的差别。
- **建议**：直接沿用已确认的选择规则、同名分类合并、同名笔记重命名/覆盖/跳过、后端事务复制、分类与笔记共享顺序。复用现有 dnd-kit，补齐落点和排序持久化，不另建排序模式。
- **定位**：`docs/superpowers/specs/2026-09-04-cross-project-note-import-and-ordering-design.md`；`frontend/src/features/writing/components/note-import-dialog.tsx`、`note-tree.tsx`；`backend/app/storage/services/note_transfer_service.py`、`note_service.py`；`backend/app/storage/models/note.py`、`backend/app/api/routers/notes.py`。
- **已确认边界**：来源项目只读；新笔记使用默认状态；不增加“保留锁定/隐藏状态或全部重置”选项。章节、世界书已有排序，不重复实现。
- **最小验收**：选择性复制、一个同名冲突、失败事务回滚、拖拽后刷新保留顺序。只保留保护数据安全的少量定向测试。
- **工作量**：中到大。

## P2：体积、性能与可维护性

### T06 区分首屏体积、安装体积与 PWA 下载量

- [x] 建立一次可复用的构建体积记录，再按测量结果优化。
- **完成记录（2026-09-05）**：生产构建为 34.16 MiB（JS 11.27、CSS 1.93、字体 20.86）；PWA 安装预缓存由 130 项/12.76 MiB 收敛为应用壳依赖 16 项/4.20 MiB，其他静态 chunk 访问后按需缓存。安装体积未因本项改变。
- **证据**：`frontend/scripts/generate-precachelist.mjs` 会枚举全部构建文件，排除字体、source map 和少数固定文件；`sw.js` 安装时 `cache.addAll(precacheList)`。动态拆分的 JS 仍在预缓存集合中，拆包不一定减少 PWA 首次后台下载量。
- **建议**：分别记录最终构建目录、JS/CSS、字体、首屏请求和预缓存总量；将预缓存分成必要应用壳与可按访问缓存的功能资源。已有离线访问要求要保留明确说明。
- **定位**：上述文件；`frontend/vite.config.ts`、`desktop/electron-builder.yml`、`backend/hatch_build.py`。
- **最小验收**：同一配置下前后各测一次，给出实际字节数；不把 `node_modules` 大小当安装包大小，不把分 chunk 当总量减少。
- **工作量**：小到中；先测量，不预设压缩百分比。

### T07 页面和重型渲染能力按需加载

- [x] 评估写作、角色、世界书、提示词页面的路由懒加载，以及公式/图表渲染的触发式加载。
- **完成记录（2026-09-05）**：写作、角色、世界书、提示词与仪表盘均按路由懒加载；数学和 Mermaid 插件仅在正文出现相应语法时加载。主 `index` JS 从约 3.31 MiB 降到 1.23 MiB（未压缩构建产物）。
- **证据**：`main.tsx` 中 Dashboard 使用 `lazy`，其他上述页面静态导入；`frontend/src/components/streaming-markdown.tsx` 静态引用 math、mermaid 插件。实际首屏依赖占比应通过构建图确认，不能仅据 import 断言库全部内联。
- **建议**：先拆明确的页面边界，避免公共 barrel 导出把模块重新拉回入口；公式、图表仅在确有内容时加载，预留合理占位。与 T06 的缓存策略一起评估。
- **已有优化**：`frontend/src/lib/limited-code-highlighter.ts` 已限制语言并动态导入，不再列“增加高亮语言白名单”为新任务。
- **最小验收**：普通项目列表首屏不加载未使用页面；进入页面、代码块、公式和图表各能正常展示；比较首屏字节数。
- **工作量**：中。

### T08 字体资源分组，保留离线使用能力

- [x] 评估按用户选用字体加载 CSS，以及可选字体包对发行体积的收益。
- **完成记录（2026-09-05）**：11 组字体改为按当前正文/代码字体动态加载，主 CSS 从约 1.58 MiB 降到 0.76 MiB；字体文件仍随完整发行提供并可离线使用，因此总安装体积保持不变。可选字体包留待发行方案确定后再做。
- **证据**：`main.tsx` 静态导入 11 组字体样式；`font-utils.ts` 已主动加载配置字体。构建后处理已删除非 WOFF2 文件，并将字体排除出预缓存，所以“只保留 WOFF2”和“排除字体预缓存”已经做过。
- **建议**：先测最终 WOFF2 与字体 CSS 总量；按字体族拆样式，避免无条件解析所有字体声明。安装包可评估默认字体加可选包，但必须提供系统字体回退与离线预装方案。不要直接裁掉中文字符集或撤销现有字体选择。
- **定位**：`frontend/src/main.tsx`、`frontend/src/lib/font-utils.ts`、`frontend/scripts/generate-precachelist.mjs`、`desktop/electron-builder.yml`。
- **最小验收**：常用中文与生僻字、字体切换、离线重启均可读；分别记录 CSS 和安装资源变化。
- **工作量**：中。

### T09 分阶段拆分会话 Hook 和通用 API 客户端

- [ ] 按职责增量拆分，优先随相关功能修改进行，不整体重写 Agent。
- **证据**：基线中 `frontend/src/features/assistant/hooks/use-agent-session.ts` 为 1581 行，`frontend/src/lib/api-client.ts` 为 2943 行；后者包含笔记、记忆等不同领域请求。功能清单已提示会话状态、事件恢复与 API 定位复杂。
- **建议**：API 先按领域迁移并保留原入口再导出；会话 Hook 先理清事件订阅/去重、恢复、排队/取消、UI 状态的实际依赖，再抽取一个边界。大文件本身不是性能故障，目标是降低修改耦合。
- **最小验收**：每次只搬一个职责，保持参数、响应、事件时序与缓存键一致；复用已有相关检查，重点保留断线恢复和取消竞态覆盖。
- **工作量**：中到大，分批执行。

### T10 评估后端可选依赖与轻量发行方案

- [x] 先测依赖安装占用与 import 链，再决定是否将本地模型或非默认供应商变为可选安装。
- **完成记录（2026-09-05）**：当前 Windows 虚拟环境中 LanceDB 包目录约 144.58 MiB、PyArrow 80.62 MiB、FastEmbed 0.34 MiB（不含其下载模型）；检索引擎启动链直接导入 LanceDB/PyArrow，本地模型注册和客户端依赖 FastEmbed。为保留现有开箱即用检索，本批次不从默认依赖删除它们。轻量发行需先增加能力检测和发行变体，不能作为无提示裁剪。
- **证据**：`backend/pyproject.toml` 默认依赖包含 `fastembed`、`lancedb`、`pyarrow` 和多家模型提供商适配库。`backend/app/models/builtin.py` 会注册内置本地 Embedding/Rerank 模型，裁剪依赖会影响已承诺的开箱即用行为。
- **建议**：考虑完整发行和轻量发行并存；轻量模式提供能力检测与缺失组件提示。检查启动注册和导入链后才能做 extras，不能仅从依赖表删除库。
- **最小验收**：记录各发行实际安装体积、启动时间；轻量模式能完成远程模型会话，完整模式的本地检索仍正常。
- **工作量**：大；收益待测，不建议作为第一项重构。

### T11 笔记文件导入增加请求竞争保护和真实进度状态

- [x] 防止旧预览覆盖新选择，并避免导入开始就显示 100%。
- **完成记录（2026-09-05）**：文件和项目预览均使用请求序号丢弃迟到响应；关闭窗口会使在途响应失效；导入阶段改为不定进度，不再显示虚假 100%。
- **证据**：`note-import-dialog.tsx` 的 `handleFileSelect` 异步完成后直接更新预览，没有请求序号或取消检查；关闭时 `resetState()` 不能阻止已发出的 Promise 回写；文件选择区加载中仍可再次触发。导入中 `<Progress value={100}>` 是固定值。
- **影响**：存在快速选择 A/B 或关闭后旧响应回写的竞态风险；尚未动态复现，需最小验证后确定修复细节。
- **建议**：使用请求序号/AbortSignal 保护预览结果；禁止不适合并发的操作。没有真实进度接口时只显示不定进度，成功响应后才显示完成。可随 T05 一并处理。
- **最小验收**：让 A 请求慢于 B，只显示 B；关闭再打开没有旧结果；执行中不会显示虚假的完成比例。
- **工作量**：小。

### T12 增加会话消息内容检索

- [x] 在已有任务标题搜索之外，提供用户消息/回复正文检索，并能跳到匹配消息。
- **完成记录（2026-09-05）**：任务搜索同时匹配标题与当前 Agent 运行消息中的用户/助手正文，返回首个命中消息 ID 和摘要；全部任务页展示摘要，打开后通过虚拟消息列表定位到对应消息。删除任务仍沿用同一清理链路，不新增独立搜索索引。
- **证据**：`backend/app/storage/repos/task_repo.py` 的 `search_query` 当前仅匹配 `Task.title`；`all-tasks-page.tsx` 已有搜索入口。消息导航解决按轮次跳转，不能代替忘记所在会话时的正文搜索。
- **建议**：先提供当前项目范围的消息检索，返回任务、消息 ID、时间和摘要；与导航稳定 ID 及历史加载链路衔接。数据量变大后再评估 SQLite FTS，避免一开始加载全部聊天到浏览器。
- **定位**：上述文件；`backend/app/api/routers/tasks.py`、`backend/app/storage/models/task_message.py`；消息导航分支。
- **最小验收**：只有正文含关键词时也能命中，并正确打开目标会话和消息；删除任务后不出现陈旧结果。
- **工作量**：中；新增功能候选。

### T13 更新功能地图，减少误导性入口和重复文档

- [x] 更新功能地图基线与状态，保留真实可定位的职责入口。
- **完成记录（2026-09-05）**：功能地图更新到当前改进分支，补充实现/设计状态说明、持久化笔记排序、跨项目导入和会话消息导航入口；贡献指南替换空占位，给出前后端启动与定向验证命令。
- **证据**：`feature-map.md` 基线为 `2eae5db`；它将 `agent-sidebar.tsx` 与会话 Hook 一并提示为高复杂度总装配点，但当前前者只有 251 行、后者 1581 行，应按实际职责更新。`frontend/src/routes/index.ts` 只有 `export {}`；路由实际在 `main.tsx`。`CONTRIBUTING.md` 当前仅有 `TODO`；子项目 README 大量重复产品介绍。
- **建议**：功能地图增加“主分支可用/分支实现/仅有设计”状态；贡献指南写清 Windows 启动、前端命令和少量定向验证方式；子项目 README 链接产品介绍并补自身启动与构建说明。空路由占位是否移除取决于调用关系，不为文件整齐而搬迁。
- **最小验收**：路径能打开；按贡献指南可启动；新需求能找到唯一主入口，不把计划当实现。
- **工作量**：小。

### T14 统一 Windows 开发启动指引

- [x] 在开发说明中明确使用应用 CLI，或显式指定兼容 ZMQ 的事件循环。
- **完成记录（2026-09-05）**：`CONTRIBUTING.md` 明确使用 `python -m app.cli serve`，说明隔离数据目录，并标注不要用会绕过 Windows 兼容逻辑的直接 Uvicorn 命令。
- **证据**：`backend/app/cli.py` 已通过 `_get_uvicorn_loop_factory()` 在 Windows 选择 Selector 事件循环。此前界面验证直接运行 `python -m uvicorn app.main:app` 时出现 Proactor/ZMQ 错误；该命令绕过了现成兼容逻辑，不能据此判定正式启动必然故障。
- **建议**：开发与验证优先使用 `python -m app.cli serve`，并显式设置验证用数据目录；无需仅为错误的启动方式强行增加依赖。
- **最小验收**：用文档命令启动后，健康检查、Socket 和后台任务基础状态可用，不出现该事件循环错误。
- **工作量**：小；可和 T13 同步。

## P3：可选新增功能

### T15 明确 Agent 记忆能力，再决定是否补管理页面

- [ ] 确定记忆与规则、笔记的边界，再选择补 UI、补上下文接入或清理废弃接口。
- **证据**：`backend/app/api/routers/agent_memories.py` 有 CRUD/排序且在 `main.py` 注册；`frontend/src/lib/api-client.ts` 有配套函数。设置目录中未发现对应管理界面，在本次检查的 `backend/app/agent_runtime/context` 中也未找到 AgentMemory 注入引用。后者只是静态检索结果，实施前还需追踪其他调用方，不能假定它已经影响模型输出。
- **建议**：如保留该能力，先实现记忆来源、查看/编辑/删除与实际生效范围；项目范围和全局范围需明确。优先复用规则或笔记能表达的需求，避免新增一套用户难以区分的资料库。
- **最小验收**：可解释一条记忆是否进入本轮上下文；修改/删除后生效且不串项目。
- **工作量**：中到大；属于产品决策候选。

### T16 在已有桌面备份上补定期备份与保留策略

- [ ] 评估定期本地备份、最近成功时间和保留数量的简洁设置。
- **证据**：`desktop/src/main/data-manager.ts` 已有备份/恢复；`backup-manifest.ts` 已有大小与哈希校验；`ipc.ts` 已有入口和进度。因此不应重新立项“新增备份功能”。本次未完整核查所有调度入口，实施前确认是否已有相关设置。
- **建议**：复用现有备份操作和运行时协调，提供可配置周期与失败反馈；Web 自托管场景另行明确服务器备份责任。先展示将回收的旧备份范围，避免无边界清理。
- **最小验收**：一次定时备份成功、一次失败提示、恢复一次包含数据库和文件资产的副本；验证保留策略只作用于该功能创建的备份。
- **工作量**：中；新增体验候选。

## 执行与维护约定

- 每次只挑一个可独立交付的任务；更新复选框时记录提交、验证结果和剩余限制。
- 优先 T01–T03，再处理 T04/T05；缩体积从 T06 的基线开始，T07/T08 按实际收益排序。
- 已取消的编辑器页码/行号跳转，以及笔记导入状态选择，不重新列入需求。
- 不重复实现已经存在的草稿恢复、桌面备份校验、章节/世界书排序、有限语言高亮和编码表动态加载。
- 文档调整不跑全量测试；行为修改复用现有定向检查。新增测试只保护数据保存、事务完整性、缓存更新和关键状态竞争，不为文案与简单样式增加大量测试。
